# Mojo Advent Of Code

This folder uses uv, but it uses mojo-compiler as a dependency.
Also includes `mojo` if you want to work on Visual Studio Code (It needs mojo-lsp-server included in `mojo` package).
